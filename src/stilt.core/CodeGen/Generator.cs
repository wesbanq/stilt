namespace stilt.CodeGen
{
    // INCOMPLETE: code generation (pipeline stage 5) is a stub. This stage is meant to lower IR Blocks into the
    // actual Minecraft datapack output — emitting one .mcfunction per named block and the commands for each
    // instruction. Nothing here is wired into the pipeline yet.

    //command limit 65536

    public class MCString
    {

    }
}

